version: 2

models:
  - name: dim_customers
    description: "Dimension table containing unique cardholders."

    columns:
      - name: cardholder_id
        tests:
          - not_null
          - unique

      - name: country
        tests: []
